<?php 
if(isset($this->apiData) && count($this->apiData)) {
    foreach($this->apiData as $apiKey => $message) {
        echo strtoupper($apiKey), ' "', $message, '"', chr(10);
    }
} else {
    echo 'ERROR "No output data!"', chr(10);
}